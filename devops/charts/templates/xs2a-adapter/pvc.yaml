{{- if .Values.adapter.customConfig }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: xs2a-adapter-custom-config
  labels:
    app: xs2a-adapter
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
{{- end }}
